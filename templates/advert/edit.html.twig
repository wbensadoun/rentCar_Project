{% extends 'base.html.twig' %}

{% block title %}Hello CustomerController!{% endblock %}

{% block body %}
    <div class="row">
        <div class="col-4">
            <a class="btn btn-danger" href="{{ path('list_adverts') }}"> Retour </a>

        </div>
    </div>
    <div class="form-group">
    {{ form_start(form) }}

        	<div class="row">
					<div class="col-offset-6 col-6">
                    		{{ form_row(form.titre) }}
					</div>

               		<div class="col-offset-4 col-8">
                    		{{ form_row(form.description) }}
           			</div>

					<div class="col-offset-6 col-6">
							{{form_row(form.car) }}
					</div>
					
              		<div class="col-offset-6 col-6">
                    		{{ form_row(form.prix) }}
                	</div>
            </div>   
               
    {% if action == "create" %}
        <button class="btn btn-primary" type="submit">Créer</button>
    {% elseif action == "edit" %}

        <button class="btn btn-primary" type="submit">Modifier</button>
    {% endif %}
    {{ form_end(form) }}
    </div>
{% endblock %}